Steam has been installed. To get proper GPU support, install one of the following meta-packages:

- For AMD/Intel (Mesa) GPUs: sudo xbps-install steam-bin-mesa
- For NVIDIA GPUs:           sudo xbps-install steam-bin-nvidia